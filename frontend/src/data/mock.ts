/**
 * Prototype seed data — NOT used by production pages (phase B).
 * Kept for local UI experiments only. Import from `data/demo.ts` for login hints.
 */
export { DEMO_CREDENTIALS } from './demo';

/** @deprecated Phase B: do not import into pages */
export const MOCK_ISOLATED = true;
